export const calendarHelper = {
    /**
     months : {
        {
          name: 'June',
          year: 2018,
          days: [
            {
              date
              events:[
                string,
              ]
            }
          ]
        }

     }
     */
    getCalendarInfo(date, fixtures) {
        const months = [];
        const currentMonth = 0;
        const currentDay = 0;
        return {months, currentMonth, currentDay};
    }
};
